- sca: Scanned 6 local repos with osv-scanner v2 (ghcr.io/google/osv-scanner:latest) against committed lockfiles, plus grep of package.json specs and installed node_modules. Repos: OrbisVoice2026 (root pnpm-lock, monorepo apps/web+api+gateway), myorbisbiz (pnpm-lock), myorbis-content-engine (pnpm-lock), MyOrbisReviews (package-lock), storefront clone at scratchpad/sf-repo (pnpm-lock). Network to registry.npmjs.org=200, docker 29.3.1 available. RESULTS: content-engine=0 vulns (not yet deployed per project memory). myorbisbiz=27 vulns (15 High). voice monorepo=92 vulns (2 Critical/46 High). storefront=35 vulns (2 Critical/14 High). reviews=7 vulns (4 High). NOTE the storefront /tmp scratchpad path would not bind-mount into docker (session tmpfs); worked around by copying lockfile to /var/tmp before scanning. GAPS/could-not-verify: (1) Could NOT confirm the ACTUAL next.js version running in live containers (products.myorbisresults.com / app.myorbisvoice.com / myorbisbiz.com) — READ-ONLY guardrail forbids docker exec into prod; findings reflect committed lockfiles, which is what a rebuild/redeploy installs. (2) osv-scanner queries osv.dev so results reflect advisories as of scan time. (3) MyOrbisReviews repo is the vite SPA only; the separate reviews-api container (4200 lo) has no package.json in this clone, so its deps were not scanned. (4) Vendored 3rd-party dev tools inside OrbisVoice2026 (goose/, claudecodeui/, superpowers/, remotion/, playwright/, distill/, impeccable/) have their own lockfiles with old deps (e.g. claudecodeui express 4.18 / node-fetch 2) but are NOT deployed MyOrbis product surfaces — excluded as out-of-scope local tooling. (5) MyOrbisLocal has no package.json (docs/mockup only) — nothing to scan.
- container: Scope: docker inspect (read-only) of all 21 MyOrbis containers across box1 (contabo-voice 147.93.183.4) and box2 (contabo-local 109.123.249.34), plus Trivy HIGH/CRITICAL image scans of the 10 app images. Non-MyOrbis containers (bps_zf*, zerofees*, applightbox*, yt_transcriber*) were NOT touched. Effective runtime UID verified by `docker exec ... id` on the 8 app/edge containers; /var/tmp writability verified live. Next.js versions read from inside containers.

RUNTIME POSTURE (fully covered, all 21 containers inspected): Every container has RestartPolicy=unless-stopped (compliant), Privileged=false (good), CapAdd=[] (no added caps, good), but ALSO CapDrop=[] (nothing dropped), SecurityOpt=[] (no seccomp/no-new-privileges), ReadonlyRootfs=false (writable rootfs), and /var/tmp writable — on ALL of them. No container is hardened. Ports published to 0.0.0.0: ONLY edge-caddy (80/443); every other published port is bound to 127.0.0.1 (hub-api 4100, keycloak 8080/9000, storefront 4300, reviews-api 4200, reviews-web 4400) or unpublished/internal-network-only — good network hygiene. App containers reach the internet only via edge-caddy reverse proxy (app.myorbisvoice.com, api.myorbisvoice.com, myorbisbiz.com, products/reviews.myorbisresults.com), so 'internet' exposure below means app-layer reachable through the proxy even though the port is loopback.

ROOT audit (Config.User empty/0 AND verified uid=0 via exec): ROOT = myorbisvoice-web, myorbisvoice-api, myorbisvoice-gateway, myorbisvoice-render, myorbis-hub-api (box1); myorbis-reviews-api, myorbis-reviews-web, myorbisbiz-app, edge-caddy (box2); all postgres/redis containers. NON-root (remediated/good) = myorbis-storefront-api (uid=1000 node), myorbisvoice-leadengine (leadengine), myorbis-id-keycloak (1000), myorbisvoice-n8n + n8n-worker (node).

POSITIVE / breach remediation confirmed: myorbis-storefront-api — the cryptojacked container — now runs as uid=1000(node) (was root) and Next.js is 15.5.22 (was the vulnerable 15.1.6). voice-web Next.js=15.5.18, myorbisbiz Next.js=16.2.9 — all past the CVE-2025-29927 / RCE window. The app-code RCE vector appears patched; the residual risk is defense-in-depth (root + writable rootfs would let a FUTURE Next/Node RCE persist a miner exactly as before).

GAPS / not fully covered: (1) Trivy per-image results for myorbisvoice-api/gateway/web/render lost their exact 'Total:' count line because the scan command piped through `tail -40` and those tables exceed 40 lines — the individual CVE rows and packages ARE captured (node-tar, undici, ws) and are reported, but exact HIGH totals for those 4 images are approximate. hub/storefront/reviews/reviews-web/leadengine/biz Totals are exact. (2) postgres:16 / redis:7 images were not Trivy-scanned (task scoped scan to the app images only). (3) No live exploitation attempted per guardrails. Ephemeral aquasec/trivy containers ran with --rm and the helper cache volume was removed from both boxes afterward.
- secrets: Scanned 6 repos: OrbisVoice2026, myorbisbiz, myorbis-content-engine, MyOrbisReviews, MyOrbisLocal (all under /run/media/orbis/Projects/Antigravity/), and the storefront clone at scratchpad/sf-repo.

Methods run:
1) gitleaks:latest (docker run --rm) — full working-tree scan on OrbisVoice2026 TIMED OUT / was node_modules-bound (repo is large, --no-git scans every file incl. deps). GAP: gitleaks did not complete on any repo. Compensated with targeted high-signal scans below, which are more precise for "committed secrets" than a noisy full-tree gitleaks pass.
2) `git grep` over TRACKED files in every repo for real key formats: sk-, sk_live_/rk_live_, AKIA*, AIza*, SG.*, re_*, whsec_*, PEM private-key headers, xox* Slack. Result: only placeholders in docs/validators/i18n strings + the .env.prod.template literal `sk_live_` placeholder. No real values.
3) `git grep` over GIT HISTORY (up to ~400 commits per repo, all refs) for the same patterns. Result: zero real secrets ever committed in any repo.
4) Hardcoded-literal scan for password/secret/token/apikey = '...{12,}' in .ts/.js/.tsx/.yml. Only hits: apps/e2e/src/config.ts test cred 'E2eTest1234!' for a throwaway @test.local account (not real), codemagic.yaml Android signing = $CM_* env refs (not real), PasswordInput aria-label (false positive).
5) HUB/service-token / Bearer scan — all HUB_TOKEN / HUB_SERVICE_TOKEN / GATEWAY_INTERNAL_TOKEN usages read from process.env; no hardcoded token. Directly relevant to the storefront breach's 'shared HUB service token' — the Voice side does NOT hardcode it.
6) Env-dump-on-error vector (the documented breach cause): grepped every repo incl. sf-repo for JSON.stringify(process.env), ...process.env spreads, console/res.json(process.env), stack+env dumps. ZERO matches in tracked code. NOTE: this only covers committed source; runtime error-logging behavior on live prod containers was NOT inspected (out of scope / read-only, and the breach's env leak may have been runtime/Next.js-RCE driven rather than source-visible).

Positive controls confirmed: all on-disk .env files (OrbisVoice2026, myorbisbiz, content-engine) are gitignored and NOT tracked; infrastructure/docker/.env.prod.template and content-engine/.env.prod.example contain only CHANGE_ME / empty placeholders.

Not covered: gitleaks full completion (fallback used); no scan of live container filesystems/logs for injected secrets; box1/box2 prod env files not read (read-only remote scope not exercised here).
- sast: Ran semgrep (semgrep/semgrep docker image, --config=auto --severity=ERROR) on the OrbisVoice2026 monorepo: 18 ERROR results — 5 missing-user (root containers, confirmed real), 10 gcm-no-tag-length (FALSE POSITIVES — verified createDecipheriv calls do setAuthTag() e.g. system-config.service.ts:30, so GCM integrity IS enforced), 3 detected-bcrypt-hash (hashes in docs/Caddyfile, not code secrets). Targeted grep sweeps for high-value sinks (child_process/exec/spawn with interpolation, eval/new Function, dangerouslySetInnerHTML, $queryRawUnsafe/raw SQL concat, SSRF fetch/axios with user input, webhook signature validation, shared service tokens, missing route auth, tracked .env/hardcoded secrets) run across all 6 repos: OrbisVoice2026, myorbisbiz, myorbis-content-engine, MyOrbisReviews, MyOrbisLocal, and the storefront clone (sf-repo). Next.js versions checked (breach was 15.1.6 RCE): all repos now on patched trains — voice web 15.5.18, storefront 15.5.22, myorbisbiz 16.2.9. Webhook signature validation verified solid: Stripe constructEvent, Twilio validateRequest (fails-closed, TWILIO_ENFORCE_SIG on by default), HMAC + timingSafeEqual for gateway/mail/recording tokens. No exec/spawn interpolation, no eval (only wav-to-mp3.ts new Function('m','return import(m)') dynamic-import shim — safe, constant string), no raw SQL concat (only Prisma parameterized) found. Storefront tenant-scoped routes (dna save/logo/profile/documents) correctly derive tenantId from session memberships, NOT client input — no IDOR despite god-token. NOT DONE: gitleaks/trivy image scans skipped (docker image pull latency) — substituted targeted regex for sk-/AKIA/whsec_/SG./AC-SID/xoxb patterns in tracked src (none found); no git-history secret archaeology; no live HTTP/TLS/header checks (read-only code scope); myorbis-content-engine + MyOrbisReviews given lighter pass (content-engine not yet deployed per project memory, reviews serves via caddy static). apps/web/.env.production is committed but contains only NEXT_PUBLIC_* (build-inlined, public by design) — no secret exposure.
- edge-net: Enumerated ports on both box IPs via bash /dev/tcp TCP-connect probe (nmap not installed) across 18 common/service ports (22,80,443,3000,4000,4100,4200,4300,4400,4600,5000,5432,5678,6379,7000,8080,9000,3306). Checked TLS certs/issuer/expiry and headers (HSTS/CSP/X-Frame/Server/x-powered-by/www-authenticate) via openssl + curl on all 10 public hosts. SSH'd read-only into contabo-voice (box1) and contabo-local (box2) to inspect `docker ps` port publishings and the reverse-proxy configs (shared /opt/bps_zf/caddy/Caddyfile myorbis blocks on box1 — the sanctioned shared-file exception; /opt/edge/Caddyfile + conf.d on box2). Verified CF-origin bypass with --resolve.

CONFIRMED CLEAN (positive results): Every myorbisvoice/hub/id internal service is NOT published to the public interface. External probe: 5432/6379/8080/9000/4000/5000/7000 all filtered on both boxes. docker ps: myorbisvoice-postgres/redis, myorbis-hub-db, myorbis-id-db all expose 5432/6379 internal-only (no host publish); keycloak + hub-api bound to 127.0.0.1 only; api/gateway/leadengine internal-only reached solely via Caddy on docker net. Box2: storefront-api 127.0.0.1:4300, reviews-api 127.0.0.1:4200, reviews-web 127.0.0.1:4400, myorbisbiz-pg/reviews-db 5432 internal. n8n double-lock intact: myorbisvoice-n8n is internal-only and n8n.myorbisvoice.com returns 401 Basic (Caddy basic_auth). TLS: valid Let's Encrypt certs on box-hosted hosts, none near expiry (all Sep 2026), TLS1.3 negotiated.

COULD NOT verify: (1) full 65535-port sweep — only the 18-port common set was probed (nmap absent). (2) Legacy TLS1.0/1.1 acceptance — this host's openssl refuses to offer them client-side ("no protocols available"), so negotiation was inconclusive; box edges are modern Caddy which defaults to TLS1.2+. (3) The public 0.0.0.0:5678 on box1 belongs to container `zerofees-n8n-agency` (OIDC login URL = n8n.bpszerofees.com), a NON-myorbis stack — skipped per guardrails, not scanned further; noted only that myorbisvoice's own n8n is unaffected/internal. (4) Apex myorbisvoice.com (Spaceship marketing) and myorbisagents.com (Cloudflare Pages) are not box-hosted, so no origin bindings to inspect.
- identity: Keycloak (realm `myorbis`) inspected READ-ONLY by querying its Postgres DB directly (docker exec myorbis-id-db psql), because kcadm auth was correctly blocked and I did not guess/brute admin creds (per guardrails). Coverage from DB: full client inventory (client table flags), redirect_uris, web_origins, client_attributes (PKCE/logout), service-account role mappings (user_role_mapping), realm password_policy + realm_attribute brute-force settings.

VERIFIED-GOOD (no finding needed): (1) RBAC parity holds — server middleware apps/api/src/middleware/rbac.ts defines all three tiers (requirePlatformSuperAdmin / requirePlatformAdmin / requirePlatformSupport) matching UI apps/web/src/lib/auth.ts getPlatformRoleTier(); admin router apps/api/src/routes/admin.ts:34 blanket-guards every route with `authenticate, requirePlatformSupport` and adds requirePlatformAdmin/requirePlatformSuperAdmin per privileged write (95 requirePlatformAdmin + 34 requirePlatformSuperAdmin usages) — the UI hides buttons the server independently rejects; no route found relying only on UI gating. (2) Impersonation route intentionally allows Support tier per spec (audit-logged). (3) Service accounts are minimally scoped: only service-account-myorbis-voice-provisioner has an extra role, `manage-users` (from realm-management) — appropriate for provisioning, NOT realm-admin/manage-realm; agents/dashboard/voice service accounts carry only default roles. (4) The confidential clients myorbis-voice, myorbis-dashboard, myorbis-agents each have a single exact HTTPS callback (no wildcard) and a client secret. (5) Voice OIDC login/callback (apps/api/src/routes/auth-oidc.ts) is well-built: state-cookie CSRF, safeNext() blocks open-redirect on the post-login `next`, confidential client w/ secret, tokens returned via URL fragment (kept out of logs).

COVERAGE GAPS: (a) Could not enumerate via kcadm / Admin API (auth attempt blocked + no creds supplied) — findings derive from the KC backing DB, which is authoritative for config but I could not cross-check runtime token-endpoint behavior with a benign request. (b) Did not test the ROPC endpoint live (would be an auth attempt — out of scope per guardrails); the brute-force exposure is asserted from config (public client + directGrant + bruteForceProtected=false), not exploited. (c) Realm/session timeout values and refresh-token rotation policy in KC not fully enumerated. (d) The 'redirect-loop/session issue' referenced: root cause is documented in code (apps/web/src/lib/auth.ts:26-40) as a KC browser /logout confirm-screen NPE 500 on expired code (KC bug, 2026-06-21), worked around with back-channel admin revoke — reviewed, noted below as low.
- live-web: Non-destructive DAST via single curl GETs against all 10 public hosts (app/api/n8n.myorbisvoice.com, auth/hub/products/reviews.myorbisresults.com, myorbisbiz.com, myorbisvoice.com, myorbisagents.com). Checked: security headers on root + a rendered/known route per host; unauthenticated GET on admin + API data routes; GET /.env, /.git/config, /.git/HEAD; forced-error requests for stack-trace/verbose-error leakage. Loopback-bound services (myorbis-hub-api:4100, storefront-api:4300, reviews-api:4200, reviews-web:4400, id-keycloak:9000) were NOT reachable directly and were only observed through their public edge hostnames. NOT done (out of scope / would require more than single GETs): port enumeration from the host itself, TLS cipher/cert-chain analysis, authenticated route testing, any fuzzing. .env/.git 200s on reviews.myorbisresults.com and myorbisagents.com were VERIFIED as false positives — SPA/edge catch-all returns index.html (content-type text/html, 399-byte app shell), not the real files; treated as clean. hub.myorbisresults.com returned 404 on root and /dashboard (no app served on that hostname at scan time). Verbose-error probe on api.myorbisvoice.com returned clean normalized JSON errors (no stack traces). No exploitation, brute force, or writes performed.
- supply-chain: Scanned package.json + lockfiles + Dockerfiles for the 6 product surfaces: OrbisVoice2026 monorepo (root + apps/api, web, voice-gateway, render, pdf-render, lead-engine + packages/*), myorbisbiz, myorbis-content-engine, MyOrbisReviews, and the storefront clone (sf-repo). Vendored/third-party trees (playwright, goose, distill, impeccable, claudecodeui, superpowers, remotion, arbor, .claude/worktrees, .next build artifacts, voiceautomation-project-root-regenerated) were EXCLUDED as they are not MyOrbis-authored product code. MyOrbisLocal has NO node manifests (docs + HTML mockup only) — nothing to scan. Off-limits stacks (bps_zf*, zerofees*, applightbox*, yt_transcriber*, carpediem*) were not touched. Checks performed: non-registry deps (github:/git+/http tarballs) via lockfile resolution inspection; top-level install-script hooks (pre/post/install) in every product manifest — NONE found; pnpm build-script allow/deny config (.npmrc/onlyBuiltDependencies) — none set; unpinned security-critical ranges (next/react/express/prisma/jsonwebtoken); Dockerfile base images, root vs non-root execution, and curl|bash/untrusted-source install patterns — NO curl|bash found in any product Dockerfile (clean). Read-only throughout; no writes/restarts/deploys. GAPS: did not run automated CVE scanners (osv-scanner/trivy/pnpm audit) — no network-verified vuln database pass this run; version-vuln notes below are from known public CVE history, not a live scan. Did not fetch/inspect the github:cwpete61/myorbis-shared tarball contents (external fetch), so cannot confirm/deny an install script inside it — flagged on provenance risk, not confirmed payload. Transitive-dependency install scripts (blocked-by-default under pnpm 9/10) were not enumerated.